const mongoose = require("mongoose")

var Schema = mongoose.Schema
var esquemaTarea = new Schema({
    titulo:  String,
    fecha: String,
    hora: String
})

var m_tareas = mongoose.model("m_tarea", esquemaTarea)

module.exports = m_tareas